spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQLDialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
##(create, update, create-drop, validate)
spring.jpa.hibernate.ddl-auto=update

firebase.bucket-name=${BUCKET_NAME}
firebase.credentials.path=classpath:firebase-config.json


spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

jwt.secret=${TU_SECRETO_SEGURISIMO}
## 30 minutos
jwt.access-token-expiration=1800000 
#$ 7 días
jwt.refresh-token-expiration=604800000 
frontend.url=http://localhost:5173

spring.mail.host=email-smtp.us-east-1.amazonaws.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.from=placidoocana@gmail.com
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


mercadopago.access-token=TU_ACCESS_TOKEN
mercadopago.client-id=TU_CLIENT_ID
mercadopago.client-secret=TU_CLIENT_SECRET
mercadopago.integrator-id=TU_INTEGRATOR_ID

app.url.base=http://localhost:8080

logging.level.org.springframework.security=DEBUG
